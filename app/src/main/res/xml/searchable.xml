<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:label="@string/app_name"
    android:hint="@string/search"
    android:searchMode="queryRewriteFromData"
    android:inputType="text"
    android:searchSuggestAuthority="cl.afubillaoliva.lsch.search.SearchProvider"
    android:searchSuggestPath="words"
    android:searchSuggestIntentAction="android.intent.action.VIEW"
    android:voiceSearchMode="showVoiceSearchButton|launchRecognizer"
    android:searchSuggestThreshold="1"
    android:searchSettingsDescription="words"
    android:queryAfterZeroResults="true"
    android:voiceLanguage="es">
</searchable>